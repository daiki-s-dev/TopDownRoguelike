using UnityEngine;

/// <summary>
/// 敵の出現位置を示すだけのマーカー。
/// 生成ロジックは EnemyRoomManager に任せる。
/// </summary>
public class EnemySpawnPoint : MonoBehaviour
{
    // 位置だけを管理するオブジェクト
    // 生成ロジックは EnemyRoomManager に任せる
}